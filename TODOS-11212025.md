# TODOs Checklist (2025-11-21)

This checklist tracks outstanding tasks from recent Meshrabiya/Compute/Service Library work. Each item includes context for resumption by any agent.

## Checklist

- [x] **Implement real inference logic in MLKitCustomWrapper**
  - *Context*: `MLKitCustomWrapper` is intended for custom ML Kit models. The current inference logic is a placeholder. Implement actual ML Kit custom model inference, including model loading, input/output handling, and error management.
  - *Completed*: Implemented real ML Kit custom model inference using TensorFlow Lite Interpreter, with model loading, bitmap conversion, output handling, and error management. No errors found after implementation.
  - *Commit*: See workspace edit for `/Meshrabiya/lib-meshrabiya/src/main/java/com/ustadmobile/meshrabiya/service/ml/MLKitCustomWrapper.kt` on 2025-11-21.

- [x] **Ensure ML Kit services are registered in the Service Library at runtime**
  - *Context*: ML Kit services must be discoverable and usable via the Service Library. Confirm registration occurs at app/service startup and is visible to other modules.
  - *Completed*: All available ML Kit services (native and custom) are now registered in the Service Library at runtime during UnifiedMLServiceManager initialization, ensuring discoverability and module access. No errors found after implementation.
  - *Commit*: See workspace edit for `/Meshrabiya/lib-meshrabiya/src/main/java/com/ustadmobile/meshrabiya/service/ml/UnifiedMLServiceManager.md` on 2025-11-21.

- [x] **Proceed with runtime integration for any specific executor**
  - *Context*: Executors must be integrated and invoked at runtime for supported tasks. Confirm integration for all planned executors and document any missing ones.
  - *Completed*: All planned executor implementations (PythonExecutor, JVMExecutor, MLNativeExecutor, WorkflowExecutor, JSExecutor, TaskExecutor interface) exist and are integrated in IntelligentDistributedComputeService. No missing executors found; runtime invocation is present and aligned with canonical workflow.
  - *Commit*: See workspace for `/Meshrabiya/lib-meshrabiya/src/main/java/com/ustadmobile/meshrabiya/service/compute/executor/` and `IntelligentDistributedComputeService.kt` on 2025-11-21.

- [ ] **TaskManager.kt: Implement all TODOs**
  - *Context*: Complete file retrieval, sandbox/container creation, result file storage with permissions, completion notification, resource polling, and integration with compute engine. Review all TODOs and ensure real logic is present.

- [ ] **ServicePackageManager.kt: Implement signature verification and any remaining TODOs**
  - *Context*: Ensure cryptographic signature verification is implemented for service packages. Complete any other outstanding TODOs in the file.

- [ ] **DistributedStorageManager.kt: Ensure accessScope is explicitly handled in storeFile() and throughout storage logic. Confirm hybrid encryption is fully implemented.**
  - *Context*: Storage logic must respect accessScope for all file operations. Hybrid encryption (combining symmetric and asymmetric) must be present and tested.

- [ ] **Integration Points & Messaging: Implement or complete all message handler logic and ensure all message types are registered and used in the system.**
  - *Context*: Both `MessageType.kt` and `MeshEcosystemMessage.kt` exist. Confirm all message types are registered, handlers are implemented, and messaging flows are complete.

- [ ] **Executors & Compute Engine: Confirm existence and completeness of all executor implementations. Notify of any missing executors as per the plan. Confirm existence and completeness of compute engine and resource polling logic. Implement missing methods.**
  - *Context*: Review all planned executors, ensure they exist and are complete. Validate compute engine and resource polling logic. Implement any missing methods.

- [ ] **Constants & Utility: Confirm presence of all required constants (RESOURCE_MONITORING_INTERVAL_MS, TASK_COMPLETION_RETRY_INTERVAL_MS, etc.). Add if missing.**
  - *Context*: Review all modules for required constants. Add any missing constants and document their usage.

---

*Each item should be checked off and annotated with completion details and commit references as work progresses.*
